version: '3'
services: 

  website:
    build: ./serv
    container_name: server
    ports:
      - "80:80"
      - "443:443"

  bot:
#     image: d2bc806cf339
    build: ./bot
    container_name: bot
    volumes:
      - "./bot/app/database/data.sqlite:/app/database/data.sqlite"
    ports:
      - "8443:8443"
