version: '3'
services: 

  website:
    build: ./serv
    container_name: serv
    ports:
      - "80:80"
      - "443:443"

  bot:
    build: ./bot
    container_name: bot
    volumes:
      - "./bot/app/database/data.sqlite:/app/database/data.sqlite"
    ports:
      - "8443:8443"
